import { DaysOptionPipe } from './days-option.pipe';

describe('DaysOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
